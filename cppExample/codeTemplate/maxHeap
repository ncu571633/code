#include <iostream>
#include <queue>

int main()
{
    std::priority_queue<int> maxHeap;
    for (int i=0; i<10; i++)
        maxHeap.push(i);
    
    while(!maxHeap.empty())
    {
        std::cout << maxHeap.top() << std::endl;
        maxHeap.pop();
    }
    
    return 0;
}


